import type {Snippet} from "svelte"

export type Icon = Snippet

export {file, archive, archive_opened, text_file, image_file, cloud_file} from "./files.svelte"
export {asterisk, lamp, flask, tag, spiral, eye, invisible_eye} from "./interface.svelte"